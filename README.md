# RawHook
The key hook method created based on the new process is suitable for most applications that need to obtain key input asynchronously.

Based on .NET Framework 4.8.1 (using C# 9.0+ features)
===
